在 Python 里**创建文件**非常简单，核心用内置的 `open()` 函数就能搞定，不需要额外装库，我给你整理了最常用、最实用的几种写法，直接复制就能用。


### 总结
1. 最简单创建文件：`with open("文件名", "w") as f: pass`
2. 推荐永远用 `with open()`，会自动关闭文件，安全不报错
3. 中文一定要加 `encoding="utf-8"`，否则会乱码
